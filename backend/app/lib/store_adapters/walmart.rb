module StoreAdapters
  # Placeholder — implemented in issue #6. Inherits Base, so #resolve/#check
  # raise NotImplementedError until then. Registered so #6 is drop-in.
  class Walmart < Base
  end
end
