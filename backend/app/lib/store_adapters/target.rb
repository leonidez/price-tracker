module StoreAdapters
  # Placeholder — implemented in issue #7. Inherits Base, so #resolve/#check
  # raise NotImplementedError until then. Registered so #7 is drop-in.
  class Target < Base
  end
end
