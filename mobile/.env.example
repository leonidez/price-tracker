# Copy to .env (git-ignored). In dev, point at your Mac's LAN IP so the phone
# on the same Wi-Fi can reach the backend. Expo Go permits plain http in dev.
EXPO_PUBLIC_API_URL=http://192.168.1.10:3000
EXPO_PUBLIC_API_TOKEN=dev-secret
